{
  "PreToolUse": [
    {
      "matcher": "Bash(git commit:*)",
      "hooks": [
        {
          "type": "command",
          "command": "bash -c 'cd $(git rev-parse --show-toplevel) && uv run ruff format src/ && git add -u'",
          "description": "Auto-format Python code with ruff before commits"
        }
      ]
    }
  ]
}
